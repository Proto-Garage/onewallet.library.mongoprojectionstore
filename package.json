{
  "name": "onewallet.library.mongoprojectionstore",
  "version": "0.0.5",
  "description": "ProjectionStore implementation for MongoDB",
  "main": "build/index.js",
  "scripts": {
    "clean": "rimraf build/",
    "build": "npm run clean && tsc",
    "lint": "tslint --project tsconfig.json"
  },
  "author": "High Output Ventures",
  "devDependencies": {
    "@types/mongoose": "^5.5.0",
    "husky": "^2.2.0",
    "onewallet.library.framework": "git+https://github.com/Proto-Garage/onewallet.library.framework.git#v1.10.2",
    "rimraf": "^2.6.3",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run build && git add build/",
      "pre-push": "npm run lint"
    }
  }
}
